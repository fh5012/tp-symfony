{% extends 'base1.html.twig' %}
{% block title %}Edit_Product{% endblock %}
{% block body %}
<div class="container form-event my-4">
    {{ form_start( our_form, { attr: { novalidate: 'novalidate' } } ) }}
					<div class="row">
							<div class="col-12">
								{{ form_row( our_form.name ) }}
							</div>
				  		
						<div class="col-md-6 col-12">
							{{ form_row( our_form.price ) }}
						</div>
			  		</div>
			  			 <div class="col-md-6 col-12">
							{{ form_row( our_form.description ) }}
						</div>
						{{ form_row( our_form.submit ) }}
			  			</div>
			  			</div>
					</div>
                    {% endblock %}